# CodeNest Backend

Node.js/Express API server for CodeNest monolithic application.

## Quick Start

1. Install dependencies:
   ```
   npm install
   ```

2. Start the development server:
   ```
   npm run dev
   ```

---

Configuration and data storage are located in the `/data` folder at the repo root.
