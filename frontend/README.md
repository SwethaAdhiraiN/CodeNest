# CodeNest Frontend

React single-page application for CodeNest monolithic platform.

## Quick Start

1. Install dependencies:
   ```
   npm install
   ```

2. Run the development server:
   ```
   npm start
   ```

---

API endpoints are served by the Express backend at `/api/*`.
